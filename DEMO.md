# Nature Memories demo

英文版回忆主题交互样片：闭合显示用户提供的灯塔照片，展开显示程序化建筑、岩岸、动态海面与实际反射。Wallpaper Studio 位于右上角，设备工具收纳到 View & device。已移除产品演示、支持作者与语言切换。当前未使用 Tripo 或 Blender，正式资产制作后置；AI 生成的界面视觉稿仅作为设计参考，不加载到运行时。

2026-09-15 照片传输版：初始只显示横向大设备。滚动第一段将设备宽高缩小到初始的 75%，候选照片从右边进入；随后设备大小固定，滚动或拖动驱动照片通过外屏边缘。反向滚动沿同一路径输出照片，回到最开始时恢复大屏、隐藏其他照片。

照片队列、实体照片网格、真实外屏图像由同一个 GSAP 进度驱动。照片在屏幕边缘的纹理坐标一致；外部网格裁掉屏内部分，屏幕补上对应图像。Melt 采用用户提供的 React Bits 噪声，保留 Intensity 0.95 / Scale 4.9 / Aberration 0.35，外屏主体使用原始 Melt 的全屏噪声溶解与大幅形变；只在边缘保留与实体照片衔接的窄区。实体照片随传输进度卷曲，并产生相应明暗变化。按钮切换为 1.1 秒，滚动可停在任意半程，反向输入立即接管。减少动态效果时关闭噪声、弯曲和惯性补间。

首页支持滚轮、触控板、拖动，以及照片区聚焦后的方向键、Home / End。Scroll to discover 可直接进入照片队列。场景展开后继续使用原来的旋转、缩放与倒带。Open memory 从照片浏览进度平滑回到灯塔大屏，再开始展开。

本次没有下载依赖或更换照片。新素材仍由 `src/iphone-duo/memory-photos.js` 管理，三张示例照片位于 `public/memories`，灯塔照片位于 `public/scenes/lighthouse/reference.jpg`。只有灯塔已绑定 3D 场景；其他照片继续作为照片展示，正式 3D 资产接入后置。

本版构建通过，50 项测试通过；已检查桌面、窄屏、半程传输与反向拖动，以及展开、场景缩放和倒带。

## 运行

需要 Node 22.12 或更新版本。在本目录执行：

```powershell
npm ci
npm run dev -- --host 127.0.0.1 --port 5217
```

打开 `http://127.0.0.1:5217/`，默认进入闭合的自然回忆照片。右侧标签切换壁纸工作室；`?view=wallpaper` 可直接进入工作室，`?view=memory` 进入自然回忆。

构建后再执行测试，因为原仓库的 Sites 测试会检查构建产物：

```powershell
npm run build
npm test
```

## 操作

- 自然回忆使用英文摄影回忆界面：Some places / stay with you.；展开后切换为 The lighthouse.，让场景成为视觉重点。
- View & device 收纳 Scene / Landscape / Closed、连续开合滑块、0°/90°/180°、缩放、配色、Living scene 与归位。手动修改后显示 Custom view；工作室保留左侧三项姿态滚轮。
- Open memory 从闭合照片开始，停留 0.4 秒后，用 5 秒同步展开、转向低视角并调整缩放；完成后保持 180° 展开，停留 2 秒后以 4°/秒自动环绕。设备与照片就绪后才可开始。
- 从任意手动姿态点击“播放展示”，先用 1.1 秒将当前开合、视角、缩放和屏幕方向一起平滑归位到初始闭合照片，再接照片停留与展开；已经处于初始态时直接进入照片停留。归位也支持暂停、继续或手动打断。
- 展示完成后左右拖动可环绕场景，保持海面水平、灯塔直立；按住时暂停自转，松手有短暂惯性，结束后停留 3 秒再柔和恢复。环绕可以手动关闭，弹窗和隐藏页面也会暂停环绕。
- 完成后的按钮为“倒带”，从当前环绕视角和缩放沿时间线反向收回闭合照片，再次点击“播放展示”重新展开。正放与倒带均可暂停、继续。
- 手动调整开合角度或切换姿态会退出展示。播放中拖动/缩放会中断播放；展示完成后的拖动/缩放保留倒带入口。壁纸工作室保留原有开合循环。
- 在场景或工作室中拖动手机旋转，滚轮或双指平滑缩放；焦点在设备时，方向键旋转，Shift 增大步长，+ / − 缩放。重置时自然回忆恢复闭合照片，工作室恢复横屏视角，保留素材设置。
- 底部进度条代表 0–5.4 秒的回忆时间，拖动会暂停，和设备开合角度分别控制。页面隐藏时暂停播放。原照片在独立弹窗中展示，弹窗支持 Escape、焦点约束与焦点返回。
- Sound settings 提供 Coast / Music 和音量。默认静音，仅用户主动开启后创建 Web Audio；Coast 为程序化风水声，Music 为带缓慢调制的原创环境和弦。声音随展开淡入，倒带、切到工作室或隐藏页面时淡出。
- 系统减少动态效果会关闭场景运动、自动环绕和惯性，简化界面与展示动画；保留手动检查功能。
- 两种机身配色均移除了背面 Logo 的显示。
- 两种机身配色、原五组图片/视频预设、图片/视频上传、屏幕图标、景深、视差和强度均保留，界面和错误提示统一为英文。
- 工作室上传的图片、视频、图标和参数不会被自然回忆模式覆盖。
- 要查看照片封面，可在自然回忆选“闭合”姿态；选择“场景视角”返回自然景观。

## 场景细节与当前质量

建筑按照片中的高附楼和狭长灯塔比例重做，包含灯室玻璃、细窗、栏杆、避雷针、石阶及墙面表面变化。岩岸使用 674 个石块/碎石实例与 27 个草簇，主体静态几何按材质合批。建筑和岩岸共 84,374 三角面、35 次绘制；此处未计设备、投影和反射的额外 pass。

`coastal-materials.ts` 提供 12 张程序化颜色、凹凸和粗糙度纹理。`coastal-water.ts` 使用每页自己的 Reflector、蓝紫色水体、细涟漪和接收投影的水面。反射过程中隐藏两页水面，避免递归；桌面反射贴图 1024²，窄屏 512²。冷色填充与暖色主光仅应用于自然场景，原设备展示照明不变。

这仍是照片引导的实时场景，不能等同于逐像素照片重建或 Blender 路径追踪成片；背面和被遮挡结构是合理补全。当前已接入水面运动、草簇摆动、白天克制的暖灯室变化及可选环境声/音乐。Living scene 可统一停止场景运动。

## 后续资产替换

`src/iphone-duo/diorama/page-anchors.ts` 从真实蒙皮屏幕求左右页局部坐标；每个半屏宽度归一化为 1，XY 为页平面，+Z 为场景高度。

`lighthouse-geometry.ts` 创建临时几何，返回左右页根组，以及 terrain/cabins/lighthouse 运动组。`lighthouse.ts` 负责装配和收展，`pop-up-motion.ts` 由原开合进度连续计算动画。

`memory-presentation.ts` 确定性采样闭合/场景端点，`memory-playback.js` 用 GSAP 驱动唯一的回忆时钟。倒带从当前环绕视角和缩放连续回收，多圈旋转按最短路径归位。`App.jsx` 协调播放、环绕和手动操作；界面 Motion 动画与物理时间线分工。照片入口使用参考 React Bits Tilted Card 的轻微弹簧倾斜。PhoneDevice 合并每帧渲染请求，`gesture-motion.ts` 提供按屏幕尺寸归一化的拖动、缩放阻尼和惯性计算。旧 `MemoryDemo.jsx` 及 `demo-playback.ts` 留作第一版原型参考，当前入口不加载它们。

正式 GLB 替换左右组中的模型内容，保留坐标、底部原点和运动分组即可复用交互。Tripo 与 Astra 的后续制作路线见上级目录项目计划。

照片附件在运行时仅取上半部照片作为封面，原始文件未被修改。

## 来源

基于 SkentSun/iphone-duo-depth-lab 的公开源码和设备资产，获取时 main 为 `1a04bedf042a335445383ce764e2157f2016a7ec`。通过逐文件下载恢复所需源码与资源，未包含上游 Git 历史。原授权和第三方资产说明保留在 LICENSE、THIRD_PARTY.md。
